Note: Dashboard calls /api/reviews?contractor_id=<id> — existing server /api/reviews endpoint should accept that query param.
If your server uses /api/reviews.js (Edge functions), ensure it supports filtering by contractor_id.
