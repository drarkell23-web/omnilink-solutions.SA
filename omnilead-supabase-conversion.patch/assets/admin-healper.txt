Deployment notes:
1) Ensure server env variables are set: BOT_TOKEN, ADMIN_CHAT_ID, ADMIN_SECRET
2) Place these frontend files into repo root and assets/ directory
3) Restart server on Render
4) Use Supabase SQL script previously provided to populate services table
5) Test endpoints:
   GET /api/services
   GET /api/contractors
   POST /api/lead
   POST /api/apply-badge
6) Admin uses /admin-dashboard.html
7) Contractors use /contractor-dashboard.html (they must save their profile first)
